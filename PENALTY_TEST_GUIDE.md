# 페널티 시스템 테스트 가이드 🧪

## 📋 테스트 시나리오

### ✅ 사전 준비
1. Discord 서버에 포럼 채널 생성
2. 봇 초대 및 권한 확인
3. 테스트용 계정 준비 (또는 본인 계정 사용)

---

## 🧪 테스트 1: 정상 인증 흐름

### 목표
연속 인증이 정상적으로 작동하는지 확인

### 단계
1. 포럼에 새 게시글(스레드) 생성
2. `!목표설정 테스트 목표` 입력
3. 사진 첨부 후 `!인증` 입력
4. `!상태`로 확인
   - 연속 1일
   - 성장일 D+1
   - 상태: 오리알 (EGG)

### 예상 결과
```
✅ 인증 완료!
연속 1일째 인증 중입니다!
성장일: D+1/66
상태: 오리알
```

---

## 🧪 테스트 2: 삐짐 (SULKY) 상태 전환

### 목표
1일 건너뛰면 삐짐 상태로 전환되는지 확인

### 방법 1: 실제 1일 대기 (권장하지 않음)
- 인증 후 실제로 1일을 기다림

### 방법 2: 대시보드 사용 (권장) ⭐
1. `http://localhost:5001` 접속
2. 로그인
3. **유저 목록** → 해당 유저 **수정**
4. **활성 도전 정보**에서:
   - `마지막 인증 날짜`를 **2일 전**으로 변경
   - 예: 오늘이 2026-02-09라면 → `2026-02-07` 입력
5. 저장
6. Discord에서 사진 첨부 후 `!인증`

### 예상 결과
```
✅ 인증 완료!

😤 오리가 삐쳤습니다!
3일 연속 인증하면 다시 돌아옵니다.

상태: 삐진
복구 진행: 0/3일
```

---

## 🧪 테스트 3: 삐짐 상태 복구

### 목표
3일 연속 인증으로 복구되는지 확인

### 단계
1. 삐짐 상태에서 1일차 인증
   ```
   복구 진행: 1/3일
   ```
2. 2일차 인증
   ```
   복구 진행: 2/3일
   ```
3. 3일차 인증
   ```
   💚 오리가 기분이 풀렸습니다!
   ```

### 빠른 테스트 (대시보드 사용)
1. 삐짐 상태 만들기
2. 대시보드에서 `마지막 인증 날짜`를 하루씩 변경하며 3번 인증
3. 3번째 인증 시 정상 상태로 복구 확인

---

## 🧪 테스트 4: 가출 (RUNAWAY) 상태 전환

### 목표
삐짐 상태에서 또 건너뛰면 가출 상태로 전환

### 방법 (대시보드 사용)
1. 대시보드에서 오리 상태를 `삐진 (SULKY)`로 변경
2. `마지막 인증 날짜`를 **3일 전**으로 변경
3. Discord에서 인증

### 예상 결과
```
✅ 인증 완료!

🏃 오리가 가출했습니다!
7일 연속 인증하면 돌아옵니다.

상태: 가출
복구 진행: 0/7일
```

---

## 🧪 테스트 5: 회복약 사용

### 목표
회복약으로 삐짐 상태 즉시 복구

### 단계
1. 삐짐 상태 만들기
2. 골드 확보 (대시보드에서 수동으로 10G 추가)
3. `!상점` 입력 → 아이템 목록 확인
4. `!구매 회복약` 입력
5. `!사용 회복약` 입력
6. `!상태`로 확인

### 예상 결과
```
✅ 아이템 사용 완료

💊 회복약을 사용했습니다!

💚 오리의 기분이 좋아졌습니다!
상태: SULKY → EGG (또는 현재 성장 단계)
```

---

## 🧪 테스트 6: 귀환석 사용

### 목표
귀환석으로 가출 상태 즉시 복구

### 단계
1. 가출 상태 만들기
2. 골드 확보 (대시보드에서 수동으로 15G 추가)
3. `!구매 귀환석` 입력
4. `!사용 귀환석` 입력
5. `!상태`로 확인

### 예상 결과
```
✅ 아이템 사용 완료

🔮 귀환석을 사용했습니다!

🏠 오리가 집으로 돌아왔습니다!
상태: RUNAWAY → EGG (또는 현재 성장 단계)
```

---

## 🧪 테스트 7: 잘못된 아이템 사용

### 목표
상태가 맞지 않을 때 아이템 사용 불가 확인

### 단계
1. **정상 상태**에서 `!사용 회복약` 입력
2. **정상 상태**에서 `!사용 귀환석` 입력

### 예상 결과
```
❌ 사용 불가
😤 회복약은 삐진 상태일 때만 사용할 수 있습니다.
```

```
❌ 사용 불가
🏃 귀환석은 가출 상태일 때만 사용할 수 있습니다.
```

---

## 🧪 테스트 8: 페널티 상태에서 골드 미지급

### 목표
삐짐/가출 상태에서는 골드를 받지 못함

### 단계
1. 삐짐 상태 만들기
2. 현재 골드 확인 (예: 10G)
3. 인증
4. 골드가 증가하지 않았는지 확인

### 예상 결과
- 정상 상태: 인증 시 +1G
- 삐짐/가출 상태: 인증 시 골드 변화 없음

---

## 📊 대시보드 테스트 도구 사용법

### 빠른 상태 변경
1. `http://localhost:5001` 접속
2. 유저 편집 페이지 이동
3. **활성 도전 정보**에서 변경:
   - **오리 상태**: 드롭다운에서 선택
   - **연속 도전 일수**: 복구 카운터 조작
   - **마지막 인증 날짜**: 과거 날짜로 변경하여 페널티 유발

### 예시: 삐짐 상태 즉시 만들기
```
오리 상태: 삐진 (SULKY)
연속 도전 일수: 0
마지막 인증 날짜: (오늘 날짜 - 2일)
```

### 예시: 가출 상태 즉시 만들기
```
오리 상태: 가출 (RUNAWAY)
연속 도전 일수: 0
마지막 인증 날짜: (오늘 날짜 - 3일)
```

---

## ✅ 체크리스트

- [ ] 정상 인증 흐름 확인
- [ ] 1일 건너뛰면 SULKY 전환 확인
- [ ] SULKY 3일 연속 인증으로 복구 확인
- [ ] SULKY에서 또 건너뛰면 RUNAWAY 전환 확인
- [ ] RUNAWAY 7일 연속 인증으로 복구 확인
- [ ] 회복약으로 SULKY 즉시 복구 확인
- [ ] 귀환석으로 RUNAWAY 즉시 복구 확인
- [ ] 잘못된 아이템 사용 시 에러 메시지 확인
- [ ] 페널티 상태에서 골드 미지급 확인
- [ ] 대시보드에서 상태 변경 기능 확인

---

## 🐛 버그 발견 시

이슈를 기록하고 다음 정보 포함:
1. 테스트 시나리오 번호
2. 입력한 명령어
3. 예상 결과
4. 실제 결과
5. 스크린샷 (가능하면)

---

**생성일**: 2026-02-09
**테스트 환경**: Discord.py 2.3+, Python 3.9+
